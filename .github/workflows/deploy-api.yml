name: Deploy API

on:
  push:
    branches: [main]

jobs:
  # ===========================================
  # BUILD API
  # ===========================================
  build:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Run linting (skip strict checks)
        run: npm run lint -- --max-warnings 1000 || true

      - name: Build API
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: api-build
          path: dist/

  # ===========================================
  # DÉPLOIEMENT PRODUCTION (main)
  # ===========================================
  deploy-production:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: production
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: api-build

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: Deploy API to production
        run: |
          echo "🚀 === DÉBUT DU DÉPLOIEMENT API ==="

          # Étape 1 : Créer le répertoire de déploiement
          echo "📁 Étape 1 : Création du répertoire de déploiement"
          ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} "mkdir -p /opt/air"
          echo "✅ Répertoire créé"

          # Étape 2 : Diagnostic initial
          echo "🔍 Étape 2 : Diagnostic initial du répertoire"
          ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} "ls -la /opt/air/"
          echo "✅ Diagnostic initial terminé"

              # Étape 3 : Copier les fichiers API
              echo "📋 Étape 3 : Copie des fichiers API"
              echo "=== Début de la copie des fichiers ==="
              rsync -az --delete --exclude '.git' --exclude 'node_modules' --exclude 'dist' --exclude 'front-air' --exclude 'nginx/ssl' -e "ssh -o StrictHostKeyChecking=no" ./ ${{ secrets.USER }}@${{ secrets.HOST }}:/opt/air/
              echo "=== Fin de la copie des fichiers ==="
              echo "✅ Fichiers copiés"

          # Étape 4 : Vérifier la copie
          echo "🔍 Étape 4 : Vérification de la copie"
          ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} "ls -la /opt/air/ | head -20" || (echo "⚠️ Erreur lors de la vérification" && exit 1)
          echo "✅ Vérification terminée"
          echo "📝 Progression: Étape 4 terminée, passage à l'étape 5"

          # Étape 5 : Créer le fichier .env.prod
          echo "⚙️ Étape 5 : Création du fichier .env.prod"
          ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} "
            echo 'DATABASE_URL=postgresql://postgres:air-admin-password@db:5432/air_db' > /opt/air/.env.prod &&
            echo 'JWT_ACCESS_SECRET=\"${{ secrets.JWT_ACCESS_SECRET }}\"' >> /opt/air/.env.prod &&
            echo 'JWT_REFRESH_SECRET=\"${{ secrets.JWT_REFRESH_SECRET }}\"' >> /opt/air/.env.prod &&
            echo 'NODE_ENV=production' >> /opt/air/.env.prod &&
            echo 'LOG_LEVEL=info' >> /opt/air/.env.prod &&
            echo 'ADMIN_EMAIL=\"${{ secrets.ADMIN_EMAIL }}\"' >> /opt/air/.env.prod &&
            echo 'ADMIN_PASSWORD=\"${{ secrets.ADMIN_PASSWORD }}\"' >> /opt/air/.env.prod &&
            echo 'FRONTEND_ORIGIN=https://accueil-insertion-rencontre.fr,https://www.accueil-insertion-rencontre.fr' >> /opt/air/.env.prod &&
            echo 'SMTP_HOST=smtp.gmail.com' >> /opt/air/.env.prod &&
            echo 'SMTP_PORT=587' >> /opt/air/.env.prod &&
            echo 'SMTP_USER=\"${{ secrets.SMTP_USER }}\"' >> /opt/air/.env.prod &&
            echo 'SMTP_PASS=\"${{ secrets.SMTP_PASS }}\"' >> /opt/air/.env.prod &&
            echo 'SMTP_FROM=\"${{ secrets.SMTP_FROM }}\"' >> /opt/air/.env.prod &&
            echo 'FRONTEND_URL=https://accueil-insertion-rencontre.fr' >> /opt/air/.env.prod &&
            echo 'PUBLIC_API_URL=https://accueil-insertion-rencontre.fr/api' >> /opt/air/.env.prod &&
            echo 'PUBLIC_ASSET_URL=https://accueil-insertion-rencontre.fr' >> /opt/air/.env.prod &&
            echo 'API_URL=https://accueil-insertion-rencontre.fr/api' >> /opt/air/.env.prod &&
            echo 'Fichier .env.prod créé avec succès'
          " || (echo "❌ Erreur lors de la création du fichier .env.prod" && exit 1)
          echo "✅ Fichier .env.prod créé"
          echo "📝 Progression: Étape 5 terminée, passage à l'étape 5.5"

          # Étape 5.5 : Créer le fichier .env (lecture automatique par Docker Compose)
          echo "⚙️ Étape 5.5 : Création du fichier .env pour Docker Compose"
          ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} "cp /opt/air/.env.prod /opt/air/.env && echo 'Fichier .env créé avec succès'" || (echo "❌ Erreur lors de la copie du fichier .env" && exit 1)
          echo "✅ Fichier .env créé"
          echo "📝 Progression: Étape 5.5 terminée, passage à l'étape 6"

          # Étape 6 : Vérifier le fichier .env.prod
          echo "🔍 Étape 6 : Vérification du fichier .env.prod"
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10 ${{ secrets.USER }}@${{ secrets.HOST }} "
            echo '=== Vérification de l existence du fichier .env.prod ===' &&
            if [ -f /opt/air/.env.prod ]; then
              echo 'Fichier .env.prod trouvé' &&
              echo '=== Nombre de lignes ===' &&
              wc -l /opt/air/.env.prod &&
              echo '=== Premières lignes (sans secrets) ===' &&
              head -5 /opt/air/.env.prod | sed 's/=.*/=***/' &&
              echo '✅ Fichier .env.prod valide'
            else
              echo '❌ Fichier .env.prod non trouvé' &&
              exit 1
            fi
          " || (echo "⚠️ Erreur lors de la vérification du fichier .env.prod" && exit 1)
          echo "✅ Vérification .env.prod terminée"
          echo "📝 Progression: Étape 6 terminée, passage à l'étape 7"

          # Étape 7 : Arrêter tous les conteneurs existants et nettoyer les orphelins
          echo "🛑 Étape 7 : Arrêt de tous les conteneurs existants et nettoyage des orphelins"
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 ${{ secrets.USER }}@${{ secrets.HOST }} "
            set +e
            echo '=== Arrêt des conteneurs ===' &&
            (docker stop \$(docker ps -q) 2>/dev/null || echo 'Aucun conteneur à arrêter') &&
            echo '=== Suppression des conteneurs ===' &&
            (docker rm \$(docker ps -aq) 2>/dev/null || echo 'Aucun conteneur à supprimer') &&
            echo '=== Suppression des conteneurs orphelins air-api-run-* ===' &&
            docker ps -a --filter 'name=air-api-run-' --format '{{.ID}}' 2>/dev/null | while read id; do docker rm -f \$id 2>/dev/null || true; done || true &&
            echo '=== Docker compose down ===' &&
            (docker compose -f /opt/air/docker-compose.prod.yml -f /opt/air/docker-compose.override.prod.yml down 2>/dev/null || echo 'Docker compose down ignoré') &&
            echo '=== Suppression du réseau air_backend ===' &&
            (docker network rm air_backend 2>/dev/null || echo 'Réseau air_backend non trouvé') &&
            echo '=== Nettoyage des réseaux orphelins ===' &&
            (docker network prune -f 2>/dev/null || echo 'Nettoyage réseau ignoré') &&
            echo '✅ Nettoyage terminé'
            set -e
          " || (echo "⚠️ Erreur lors du nettoyage (continuons quand même)" && true)
          echo "✅ Tous les conteneurs arrêtés et nettoyés"
          echo "📝 Progression: Étape 7 terminée, passage à l'étape 8"

          # Étape 8 : Nettoyer Docker avant le build
          echo "🧹 Étape 8 : Nettoyage Docker"
          ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} "
            docker system prune -f &&
            docker image prune -f
          "
          echo "✅ Docker nettoyé"

          # Étape 9 : Builder les images avec les variables d'environnement
          echo "🔨 Étape 9 : Build des images Docker"
          ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=60 -o ServerAliveCountMax=3 ${{ secrets.USER }}@${{ secrets.HOST }} "
            echo '=== Export des variables d environnement ===' &&
            export ADMIN_EMAIL='${{ secrets.ADMIN_EMAIL }}' &&
            export ADMIN_PASSWORD='${{ secrets.ADMIN_PASSWORD }}' &&
            export JWT_ACCESS_SECRET='${{ secrets.JWT_ACCESS_SECRET }}' &&
            export JWT_REFRESH_SECRET='${{ secrets.JWT_REFRESH_SECRET }}' &&
            echo 'Variables exportées' &&
            echo '=== Début du build Docker ===' &&
            echo 'Build de l image API...' &&
            docker compose -f /opt/air/docker-compose.prod.yml build --no-cache api &&
            echo 'Build de l image DB...' &&
            docker compose -f /opt/air/docker-compose.prod.yml build --no-cache db &&
            echo '✅ Build terminé'
          " || (echo "❌ Erreur lors du build Docker" && exit 1)
          echo "✅ Images Docker buildées"
          echo "📝 Progression: Étape 9 terminée, passage à l'étape 10"

          # Étape 10 : Démarrer les conteneurs avec diagnostic immédiat
          echo "🚀 Étape 10 : Démarrage des conteneurs avec diagnostic"
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 -o ServerAliveInterval=60 -o ServerAliveCountMax=3 ${{ secrets.USER }}@${{ secrets.HOST }} "
            set +e
            echo '=== Démarrage des conteneurs ===' &&
            docker compose -f /opt/air/docker-compose.prod.yml up -d api db &&
            sleep 10 &&
            echo '=== État des conteneurs ===' &&
            docker compose -f /opt/air/docker-compose.prod.yml ps &&
            echo '=== Vérification rapide des logs API ===' &&
            timeout 5 docker compose -f /opt/air/docker-compose.prod.yml logs --tail=20 api 2>&1 || echo '⚠️ Logs API non disponibles' &&
            echo '✅ Diagnostic terminé'
            set -e
          " || (echo "⚠️ Erreur lors du diagnostic (continuons quand même)" && true)
          echo "✅ Diagnostic des conteneurs terminé"
          echo "📝 Progression: Étape 10 terminée, passage à l'étape 10.5"

          # Étape 10.5 : Diagnostic spécifique de l'API
          echo "🔍 Étape 10.5 : Diagnostic spécifique de l'API"
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 -o ServerAliveInterval=60 -o ServerAliveCountMax=3 ${{ secrets.USER }}@${{ secrets.HOST }} "
            set +e
            echo '=== État détaillé des conteneurs ===' &&
            docker compose -f /opt/air/docker-compose.prod.yml ps -a &&
            echo '=== Vérification que l API a démarré ===' &&
            (timeout 10 docker compose -f /opt/air/docker-compose.prod.yml logs api 2>&1 | grep -q 'Nest application successfully started' && echo '✅ API démarrée avec succès' || echo '⚠️ API peut-être en cours de démarrage ou erreur') &&
            echo '✅ Diagnostic terminé'
            set -e
          " || (echo "⚠️ Erreur lors du diagnostic spécifique (continuons quand même)" && true)
          echo "✅ Diagnostic spécifique de l'API terminé"
          echo "📝 Progression: Étape 10.5 terminée, passage à l'étape 11"

          # Étape 11 : Attendre et appliquer les migrations
          echo "🗄️ Étape 11 : Application des migrations"
          ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.HOST }} "sleep 30 && docker compose -f /opt/air/docker-compose.prod.yml exec -T api npx prisma migrate deploy" || echo "⚠️ Erreur lors de l'application des migrations (peut être ignorée si déjà appliquées)"
          echo "✅ Migrations appliquées"
          echo "📝 Progression: Étape 11 terminée, passage à l'étape 12"

          # Étape 12 : Vérifier s'il y a des admins et seed si nécessaire
          echo "👤 Étape 12 : Vérification des admins et seed conditionnel"
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 -o ServerAliveInterval=60 -o ServerAliveCountMax=3 ${{ secrets.USER }}@${{ secrets.HOST }} "
            set +e
            cd /opt/air &&
            echo 'Vérification du nombre d admins...' &&
            timeout 30 docker compose -f docker-compose.prod.yml exec -T api node -e 'const { PrismaClient } = require(\"@prisma/client\"); const prisma = new PrismaClient(); prisma.user.count({ where: { role: \"ADMIN\" } }).then(count => { console.log(count); process.exit(0); }).catch(err => { console.error(\"Erreur:\", err); console.log(\"0\"); process.exit(0); });' 2>&1 | tail -1 | tr -d ' ' > /tmp/admin_count.txt || echo '0' > /tmp/admin_count.txt &&
            echo 'Résultat enregistré' &&
            set -e
          " || (echo "⚠️ Erreur lors de la vérification des admins (on assume 0)" && ssh -o StrictHostKeyChecking=no -o ConnectTimeout=10 ${{ secrets.USER }}@${{ secrets.HOST }} "echo '0' > /tmp/admin_count.txt" || true)
          echo "✅ Vérification des admins terminée"
          echo "📝 Progression: Étape 12 terminée, passage à l'étape 13"

          # Étape 13 : Lire le résultat et décider du seed
          echo "🌱 Étape 13 : Exécution conditionnelle du seed"
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 -o ServerAliveInterval=60 -o ServerAliveCountMax=3 ${{ secrets.USER }}@${{ secrets.HOST }} "
            set +e
            cd /opt/air &&
            ADMIN_COUNT=\$(cat /tmp/admin_count.txt 2>/dev/null || echo '0') &&
            echo \"Nombre d admins détecté: \$ADMIN_COUNT\" &&
            if [ \"\$ADMIN_COUNT\" -eq 0 ] || [ -z \"\$ADMIN_COUNT\" ]; then
              echo 'Aucun admin trouvé, exécution du seed...' &&
              timeout 120 docker compose -f docker-compose.prod.yml exec -T api npm run seed || echo '⚠️ Erreur lors du seed'
            else
              echo \"\$ADMIN_COUNT admin(s) trouvé(s), skip du seed\"
            fi &&
            rm -f /tmp/admin_count.txt &&
            set -e
          " || echo "⚠️ Erreur lors de l'exécution du seed (peut être ignorée)"
          echo "✅ Seed conditionnel terminé"
          echo "📝 Progression: Étape 13 terminée, passage à l'étape 14"

          # Étape 14 : Démarrer tous les conteneurs (API + Frontend + Nginx)
          echo "🚀 Étape 14 : Démarrage de tous les conteneurs (API + Frontend + Nginx)"
          ssh -o StrictHostKeyChecking=no -o ConnectTimeout=30 -o ServerAliveInterval=60 -o ServerAliveCountMax=3 ${{ secrets.USER }}@${{ secrets.HOST }} "
            set +e
            # Nettoyer les conteneurs orphelins avant de démarrer
            echo '=== Nettoyage des conteneurs orphelins ===' &&
            docker ps -a --filter 'name=air-api-run-' --format '{{.ID}}' | while read id; do docker rm -f \$id 2>/dev/null || true; done &&
            # Exporter les variables d'environnement
            export PUBLIC_API_URL='https://accueil-insertion-rencontre.fr/api' &&
            export PUBLIC_ASSET_URL='https://accueil-insertion-rencontre.fr' &&
            export API_URL='https://accueil-insertion-rencontre.fr/api' &&
            echo 'Variables d environnement exportées' &&
            # Démarrer d'abord les conteneurs frontend (astro-site et admin-dashboard) pour qu'ils soient disponibles pour nginx
            echo '=== Build et démarrage des conteneurs frontend ===' &&
            docker compose -f /opt/air/docker-compose.prod.yml -f /opt/air/docker-compose.override.prod.yml up -d --build astro-site admin-dashboard &&
            echo 'Attente du démarrage des conteneurs frontend...' &&
            sleep 15 &&
            # Tester la configuration nginx dans le contexte du réseau Docker
            echo '=== Test de la configuration nginx ===' &&
            if docker compose -f /opt/air/docker-compose.prod.yml -f /opt/air/docker-compose.override.prod.yml run --rm nginx nginx -t 2>&1; then
              echo '✅ Configuration nginx valide'
            else
              echo '⚠️ Erreur de configuration nginx détectée (on continue quand même)'
            fi &&
            # Démarrer nginx maintenant que les frontends sont prêts (forcer la recréation pour appliquer la nouvelle config réseau)
            echo '=== Démarrage de nginx ===' &&
            docker compose -f /opt/air/docker-compose.prod.yml -f /opt/air/docker-compose.override.prod.yml up -d --force-recreate nginx &&
            sleep 5 &&
            echo '=== État de tous les conteneurs ===' &&
            docker compose -f /opt/air/docker-compose.prod.yml -f /opt/air/docker-compose.override.prod.yml ps &&
            echo '=== Logs nginx (dernières 30 lignes) ===' &&
            (timeout 10 docker compose -f /opt/air/docker-compose.prod.yml -f /opt/air/docker-compose.override.prod.yml logs --tail=30 nginx 2>&1 || echo '⚠️ Impossible de récupérer les logs nginx') &&
            echo '✅ Démarrage terminé' &&
            set -e
          " || (echo "⚠️ Erreur lors du démarrage des conteneurs (continuons quand même)" && true)
          echo "✅ Tous les conteneurs démarrés"

          echo "🎉 === DÉPLOIEMENT API TERMINÉ AVEC SUCCÈS ==="

      - name: Health check API production
        run: |
          echo "🏥 Vérification de la santé de l'API"
          sleep 30
          # Tester HTTPS directement (avec redirection si nécessaire)
          # L'API retourne 401 (Unauthorized) ce qui signifie qu'elle fonctionne mais nécessite une authentification
          # 301 indique une redirection valide (HTTP -> HTTPS)
          HTTP_CODE=$(curl -sL -o /dev/null -w "%{http_code}" https://accueil-insertion-rencontre.fr/api 2>/dev/null || echo "000")
          if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "401" ] || [ "$HTTP_CODE" = "301" ]; then
            echo "✅ API en bonne santé (HTTP $HTTP_CODE)"
          else
            echo "❌ API non accessible (HTTP $HTTP_CODE)"
            echo "⚠️  Tentative avec HTTP pour diagnostic..."
            HTTP_CODE_HTTP=$(curl -sL -o /dev/null -w "%{http_code}" http://accueil-insertion-rencontre.fr/api 2>/dev/null || echo "000")
            echo "Code HTTP (via HTTP): $HTTP_CODE_HTTP"
            exit 1
          fi
